import { ActivitiesClient } from './_components/activities-client';

export const metadata = {
  title: 'Hoạt động',
  description: 'Quản lý hoạt động và lịch sử tương tác',
};

export default function ActivitiesPage() {
  return <ActivitiesClient />;
}
