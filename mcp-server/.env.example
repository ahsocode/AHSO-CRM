# URL của AHSO CRM backend
CRM_BASE_URL=https://crm.ahso.vn

# Service account credentials (tạo trong Admin → Users)
# Khuyến nghị: role MANAGER, permissions: customers.view, customers.create, customers.edit,
#              projects.view, projects.create, projects.edit,
#              calendar.view, calendar.create, ai.use
CRM_EMAIL=mcp-agent@ahso.vn
CRM_PASSWORD=your_secure_password
